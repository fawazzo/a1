# 🗄️ دليل إعداد قاعدة البيانات

## 📋 المحتويات

- جدول المستخدمين
- جدول المطاعم
- جدول الأطباق
- جدول الطلبات
- جدول التقييمات
- جدول الكوبونات
- جدول المفضلة
- جدول الإشعارات
- جدول التتبع
- جدول الفواتير

---

## 🚀 خطوات الإعداد

### **الخطوة 1: فتح phpMyAdmin**

#### **إذا كنت تستخدم XAMPP:**
1. افتح XAMPP Control Panel
2. اضغط **Start** على Apache و MySQL
3. اذهب إلى: `http://localhost/phpmyadmin`

#### **إذا كنت تستخدم WAMP:**
1. افتح WAMP
2. اضغط على الأيقونة في System Tray
3. اختر phpMyAdmin

---

### **الخطوة 2: استيراد قاعدة البيانات**

#### **الطريقة 1: من phpMyAdmin**
1. افتح phpMyAdmin
2. اضغط على **Import** في الأعلى
3. اختر ملف `database.sql`
4. اضغط **Go**

#### **الطريقة 2: من Command Line**
```bash
mysql -u root -p < database.sql
```

#### **الطريقة 3: من MySQL Workbench**
1. افتح MySQL Workbench
2. اذهب إلى **File → Open SQL Script**
3. اختر `database.sql`
4. اضغط **Execute**

---

## 📊 هيكل قاعدة البيانات

### **جدول المستخدمين (users)**
```
id              - معرف فريد
name            - اسم المستخدم
email           - البريد الإلكتروني
password        - كلمة المرور (مشفرة)
phone           - رقم الهاتف
address         - العنوان
city            - المدينة
profile_image   - صورة الملف الشخصي
is_seller       - هل هو بائع؟
created_at      - تاريخ الإنشاء
updated_at      - تاريخ التحديث
```

### **جدول المطاعم (restaurants)**
```
id              - معرف فريد
seller_id       - معرف البائع
name            - اسم المطعم
cuisine         - نوع المطبخ
description     - الوصف
rating          - التقييم
reviews_count   - عدد التقييمات
delivery_time_min - الحد الأدنى لوقت التسليم
delivery_time_max - الحد الأقصى لوقت التسليم
delivery_fee    - رسوم التسليم
min_order       - الحد الأدنى للطلب
image           - صورة المطعم
address         - العنوان
phone           - رقم الهاتف
is_open         - هل المطعم مفتوح؟
```

### **جدول الأطباق (dishes)**
```
id              - معرف فريد
restaurant_id   - معرف المطعم
name            - اسم الطبق
description     - الوصف
price           - السعر
image           - صورة الطبق
category        - الفئة
is_available    - هل متاح؟
```

### **جدول الطلبات (orders)**
```
id              - معرف فريد
user_id         - معرف المستخدم
restaurant_id   - معرف المطعم
total_price     - السعر الإجمالي
status          - حالة الطلب
delivery_address - عنوان التسليم
delivery_fee    - رسوم التسليم
coupon_code     - رمز الكوبون
discount_amount - مبلغ الخصم
notes           - ملاحظات
delivery_time   - وقت التسليم
```

### **جدول التقييمات (reviews)**
```
id              - معرف فريد
user_id         - معرف المستخدم
restaurant_id   - معرف المطعم
rating          - التقييم (1-5)
comment         - التعليق
```

### **جدول الكوبونات (coupons)**
```
id              - معرف فريد
code            - رمز الكوبون
discount_type   - نوع الخصم (percentage/fixed)
discount_value  - قيمة الخصم
min_order       - الحد الأدنى للطلب
max_uses        - الحد الأقصى للاستخدام
current_uses    - الاستخدام الحالي
expiry_date     - تاريخ الانتهاء
is_active       - هل نشط؟
```

---

## 🔑 البيانات التجريبية

### **المستخدمون:**
- أحمد محمد (عميل)
- فاطمة علي (عميل)
- محمود حسن (بائع)
- سارة إبراهيم (بائع)

### **المطاعم:**
- Pizza Palace
- Kebapçı Halil
- Tatlı Dünyası

### **الكوبونات:**
- WELCOME30 - 30% خصم
- PIZZA50 - 50 ₺ خصم
- FREEDELIV - توصيل مجاني

---

## ✅ التحقق من الإعداد

### **1. تحقق من قاعدة البيانات:**
```sql
SHOW DATABASES;
USE turkish_food_delivery;
SHOW TABLES;
```

### **2. عرض البيانات:**
```sql
SELECT * FROM users;
SELECT * FROM restaurants;
SELECT * FROM dishes;
SELECT * FROM coupons;
```

### **3. عد الصفوف:**
```sql
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM restaurants;
SELECT COUNT(*) FROM dishes;
SELECT COUNT(*) FROM orders;
```

---

## 🔧 استعادة قاعدة البيانات

### **إذا حدث خطأ، احذف وأعد المحاولة:**

```sql
DROP DATABASE turkish_food_delivery;
```

ثم استورد الملف مرة أخرى.

---

## 📝 ملاحظات مهمة

1. **كلمات المرور:** جميع كلمات المرور في البيانات التجريبية مشفرة (يجب تشفيرها بـ bcrypt في الإنتاج)
2. **الصور:** جميع الصور من Unsplash (يمكن استبدالها بصور حقيقية)
3. **الفهارس:** تم إنشاء فهارس لتحسين الأداء
4. **العلاقات:** جميع الجداول مرتبطة بـ Foreign Keys

---

## 🚀 الخطوة التالية

بعد إعداد قاعدة البيانات، يمكنك:
1. إنشاء Backend بـ Node.js + Express
2. ربط React بـ Backend
3. استدعاء API من المكونات

---

**قاعدة البيانات جاهزة للاستخدام!** ✅
